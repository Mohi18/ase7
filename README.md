# Java-JSP-Technology-based-student-Registration-and-login-page.
It uses the concept of servers page. In which with the help of a web server, we can interact with java jsp code and database. It also uses HTML code to generate web applications form in the browser. It's a desktop web application that submits the values of application form into the Database and fetches the values from Database.
